// Placeholder — el test de counter por defecto de Flutter no aplica a opoSites.
// Los tests reales se añadirán en las fases de testing.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
