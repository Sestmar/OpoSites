import 'package:flutter/material.dart';

/// Pantalla de selección de oposición — placeholder para Bloque 3.
/// La UI final va en Bloque 4.
class SelectOposicionScreen extends StatelessWidget {
  const SelectOposicionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('¿Qué oposición preparás?')),
      body: const Center(
        child: Text('SelectOposicion — placeholder Bloque 3'),
      ),
    );
  }
}
